---
description: "Browse preferences by tag"
---

# Browse by Tag

Show all tags and their counts from captured preferences.

## Instructions

Call the `list_tags` MCP tool from superwiser.

Present the tags in a readable format. To filter preferences by a specific tag, the user can use: `/superwiser:search #<tagname>`


