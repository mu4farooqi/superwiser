---
description: "Export all preferences"
---

# Export Preferences

Export all captured preferences and decisions.

## Instructions

Call the `export_preferences` MCP tool from superwiser.

- For JSON format: use `output_format="json"`
- For readable text format: use `output_format="text"`

Present the output to the user in their requested format.


